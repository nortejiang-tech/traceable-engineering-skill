---
name: traceable-engineering
description: Run document-driven, stage-gated engineering for complex software projects with ADRs, evidence-based acceptance, Coder delegation prompts, takeover control, and handoff artifacts.
disable-model-invocation: true
---

# 可追溯工程方法论

把主会话作为架构与验收中枢：先固定目标、边界和证据标准，再把当前阶段交给 Coder；根据工作区中的真实产物验收，不根据口头总结放行。

## 加载参考资料

- 在一个项目中首次使用本技能，或当前上下文没有完整方法论时，完整读取 [references/principles.md](references/principles.md)，再开始规划或验收。
- 在创建或更新 ADR、STAGE、HANDOFF、Coder 提示词或接管申请前，完整读取 [references/templates.md](references/templates.md)，采用其中对应模板。
- 不要复制两份互相独立的真相。项目内文档是项目状态的真相；本技能中的文件只定义流程和模板。

## 1. 选择本轮模式

根据用户请求和现有产物选择一种模式，并在工作更新中说明：

- **规划**：建立目标、边界、ADR 候选、阶段图和验收矩阵。
- **委派**：为唯一的当前阶段生成可直接交给 Coder 的提示词。
- **验收**：核查 Coder 已完成的改动与证据，给出 `PASS`、`FAIL` 或 `INCOMPLETE`。
- **修正**：针对验收缺口生成窄范围的 Coder 修正提示词。
- **接管**：仅在用户批准接管后，由主会话完成明确的技术难点。
- **交接**：把当前事实、验证矩阵、风险和下一步固化为 HANDOFF。

能从上下文可靠判断模式时直接推进；只有会实质改变目标、风险或授权边界时才询问用户。

## 2. 先过适用性门

检查以下问题：

1. 决策是否会跨 session 存续？
2. 失败或推翻的代价是否较高？
3. 是否存在多人、多 agent 或未来接手者？

若全部为否，说明本方法论会造成不必要的仪式化开销，改用轻量计划与直接验证。用户明确要求完整流程时继续，但记录这是用户选择。

完成标准：明确本轮需要 ADR、STAGE、HANDOFF 中的哪些层，以及为什么；不默认三层全用。

## 3. 重建可验证的项目事实

在提出方案前检查真实工作区：

1. 读取适用的 `AGENTS.md`、项目说明、既有 ADR/STAGE/HANDOFF 和相关代码。
2. 检查版本控制状态，保护用户已有和无关改动。
3. 检查可用的构建、测试、运行、日志和发布路径。
4. 把内容分成“已验证事实”“待验证假设”“未知项”；给事实附文件、命令、日志或运行态依据。
5. 对可能漂移的依赖、平台规则或外部事实进行当前验证；无法验证时标为未知，不把记忆当现状。

完成标准：所有会改变架构或验收方式的结论都有依据；未知项已进入某个阶段的行动或验证边界。

## 4. 建立阶段化架构计划

按依赖顺序给出：

1. **目标与非目标**：分别写清交付物和明确排除项。
2. **约束与不变量**：包括数据契约、失败语义、兼容性、安全、性能和运行环境边界。
3. **ADR 候选**：只为不可逆、跨阶段或推翻后需要重写的决策创建 ADR。
4. **阶段图**：每个 STAGE 必须可独立验收；同一时刻只允许一个当前阶段。
5. **验收矩阵**：把每个目标映射到具体检查、预期结果、证据形式和验证边界。
6. **风险与依赖**：为每项指定发现方式、缓解动作和归属阶段。

只把当前阶段展开到可执行粒度；后续阶段保留目标、依赖和门槛，避免 Coder 提前实施。

完成标准：每个目标都有归属阶段和验收证据；每个阶段都声明范围外事项；阶段之间没有隐藏依赖。

## 5. 创建最少必要文档

沿用仓库已有命名与目录。没有约定时，使用：

```text
docs/engineering/
├── adr/ADR-NNN-<slug>.md
├── stages/STAGE-NNN-<slug>.md
└── handoffs/HANDOFF-YYYY-MM-DD-<slug>.md
```

- 在代码实施前写好当前 STAGE 的任务书。
- 只在 Context 已有证据时把 ADR 标为 `Accepted`；否则使用 `Proposed`。
- 只有发生跨 session 或跨人接力时才创建 HANDOFF。
- 不为一次性探索、小改动或当场可验证的工作强行建档。

完成标准：文档数量与决策重量相称，且项目状态只有一个权威记录位置。

## 6. 生成当前阶段的 Coder 提示词

一次只委派一个 STAGE。提示词必须包含：

1. 角色与单一目标。
2. 必须先读的具体文件和当前工作区注意事项。
3. 范围内、范围外与禁止改动项。
4. 已确定的设计约束和不得自行改写的不变量。
5. 按顺序执行的动作，但不给出未经证实的实现细节。
6. 可判定的验收标准、验证命令和预期证据。
7. 失败停止条件，以及遇到架构分歧时返回主会话的要求。
8. 回传格式：改动文件、关键决策、命令与退出码、测试计数、运行态证据、遗留项、`git status`。

建议用户在独立 Coder 会话中使用其指定的低成本实现模型；只有当前环境已确认该型号可用时才写具体型号，不根据旧名称猜测。不要让 Coder 提交、推送、发布、清理用户改动或扩大范围，除非用户已明确授权。不要自动创建或操作另一个 Codex 会话；只生成用户可复制的提示词，除非用户明确要求线程操作。

完成标准：一个不了解主会话隐含上下文的 Coder，仅靠提示词和指定文件即可执行当前阶段并返回足够验收的证据。

## 7. 独立验收 Coder 的结果

不要把 Coder 的总结当作证据。按以下顺序验收：

1. 检查工作区状态、实际 diff、相关文件和是否存在越界改动。
2. 将每条验收标准映射到一个可观察证据。
3. 在风险允许时独立重跑关键测试、构建或运行态检查；验证交付表面，不只验证源码。
4. 区分被测对象失败、测试脚手架失败、环境失败和证据缺失。
5. 将每条判为 `PASS`、`FAIL` 或 `INCOMPLETE`；总体状态取最弱子项。
6. 把实际命令、退出码、测试计数、产物路径和边界回填到 STAGE 结果。

禁止使用“应该通过”“基本完成”或用一个通过的子项覆盖失败/未验证子项。

完成标准：每条验收标准都有状态和证据；越界改动、未验证项与技术债均被显式记录。

## 8. 修正或申请接管

验收未通过时，先判定根因属于提示词缺口、实现缺陷、工具/脚手架、环境或架构未知项。

- 对边界清晰的实现缺陷，生成一次只修一个问题簇的 Coder 修正提示词，并保留原验收标准。
- 出现架构不变量被破坏、同类缺陷经一次窄修正仍重复、高风险跨模块问题，或 Coder 无法给出可信证据时，准备接管申请。
- 接管申请必须列出失败证据、为何继续委派风险较高、主会话拟接管的精确范围、预计触碰文件和验证办法。
- 未获用户批准前不要静默接管实现。获批后只处理批准范围，并走相同的阶段门和验收流程。

完成标准：下一步是可执行的修正提示词，或一份有证据、边界明确的接管申请；不存在含糊的“再试试”。

## 9. 收口与交接

阶段完成时：

1. 回填 STAGE 的改动、验证、状态、边界和遗留。
2. 仅在证据成立时更新 ADR 状态，并记录后果与推翻条件。
3. 需要接力时创建 HANDOFF，汇总阶段状态、验证矩阵、架构现状、关键理由、技术债、文件地图和下一步责任人。
4. 在主会话中报告总体状态、关键证据、剩余风险，以及下一份 Coder 提示词或明确的人类动作。
5. 仅在用户明确授权时执行提交、推送、发布或其他外部状态变更。

完成标准：后来者无需重读完整对话即可从项目文档恢复真实状态，并能分清已验证、失败和未验证内容。
